<Window x:Class="Worksheet.App.Views.Dialogs.OscilloscopePropertiesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Oscilloscope Properties"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Channels" Margin="0,0,0,8" />

        <Border Grid.Row="1" BorderBrush="#CCCCCC" BorderThickness="1" Padding="8" MinWidth="220">
            <ScrollViewer MaxHeight="520" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ChannelsPanel" />
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="80" Margin="0,0,8,0" IsDefault="True" Content="OK" Click="Ok_Click" />
            <Button Width="80" IsCancel="True" Content="Cancel" />
        </StackPanel>
    </Grid>
</Window>
