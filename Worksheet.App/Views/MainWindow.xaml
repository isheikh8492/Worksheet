<Window x:Class="Worksheet.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Worksheet.App.Views"
        mc:Ignorable="d"
        Title="Worksheet" WindowState="Maximized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" MinWidth="240"/>
            <ColumnDefinition Width="6"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!--Left Sidebar-->
        <local:Sidebar x:Name="SidebarControl" Grid.Column="0"/>

        <!--Resizable splitter between sidebar and worksheet-->
        <GridSplitter Grid.Column="1"
                      Width="6"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="#CBD5E1"
                      ResizeDirection="Columns"
                      ResizeBehavior="PreviousAndNext"
                      ShowsPreview="True"/>

        <!--Main Content Area-->
        <Grid Grid.Column="2" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.5*"/>
                <RowDefinition Height="9.5*"/>
            </Grid.RowDefinitions>

            <!--Navigation Panel-->
            <local:Toolbar x:Name="ToolbarControl" Grid.Row="0"/>

            <!--Worksheet Area-->
            <local:WorksheetGrid x:Name="WorksheetGridControl" Grid.Row="1"/>
        </Grid>
    </Grid>
</Window>
